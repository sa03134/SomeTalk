<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sometalk">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SomeTalk.NoActionBar">
        <activity android:name=".UserManagement"></activity>
        <activity android:name=".MentorApplication" />
        <activity android:name=".Board_Love" />
        <activity android:name=".Board_Some" />
        <activity android:name=".Mentor_Ad" />
        <activity android:name=".SometalkMain" />
        <activity android:name=".Join" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditPostActivity" />
        <activity android:name=".WritePostActivity" />
        <activity android:name=".ViewPostActivity" />
        <activity android:name=".Board_Twenty" />
        <activity android:name=".Board_Teen" />
        <activity android:name=".PopularBoard" /> <!-- android:label="@string/title_activity_popular_board" -->
        <!-- android:theme="@style/Theme.SomeTalk.NoActionBar" /> -->
        <activity android:name=".Board" />
        <!--
 <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        -->
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity
            android:name=".FirstFragment"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".SecondFragment"
            tools:ignore="Instantiatable" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>